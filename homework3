def count_letter(letter, words):
  count = 0
  for word in words:
    if letter in word:
      count = count + 1
  return(count)

spisok = ['python', 'c++', 'c', 'scala', 'java']
print(count_letter("c", spisok))
